services:
  - type: web
    name: view0x-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    rootDir: backend
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production 