#!/bin/sh
# Reject commits that credit Cursor as co-author (never push cursoragent to GitHub).
if grep -qiE 'co-authored-by:.*cursoragent@cursor\.com' "$1"; then
  echo "ERROR: Remove Co-authored-by: Cursor <cursoragent@cursor.com> from the commit message." >&2
  exit 1
fi
